{
  "id": "leaderboard",
  "contexts": ["household", "shared_space"],
  "name": "Leaderboard",
  "version": "1.0.1",
  "description": "Track wins and ratings across any competition — video games, foosball, board games, and more. Elo-based ratings reward beating stronger opponents and winning with weaker teammates.",
  "entrypoint": "index.html",
  "runtime": "static",
  "category": "family",
  "tags": ["game", "competition", "stats", "fun", "family"],
  "data_access": {
    "reads": ["family.members"],
    "writes": []
  },
  "permissions": {
    "default_audience": "everyone",
    "requires_approval": false
  },
  "storage": "db",
  "preload": {
    "lb_matches": {"sql": "SELECT c.*, (SELECT COUNT(*) FROM app_leaderboard__lb_matches m WHERE m.category_id = c.id) as match_count, (SELECT MAX(played_at) FROM app_leaderboard__lb_matches WHERE category_id = c.id) as last_played FROM app_leaderboard__lb_categories c ORDER BY last_played DESC"},
    "lb_ratings": {"sql": "SELECT * FROM app_leaderboard__lb_ratings"}
  },
  "db_encryption": "off",
    "member_references": {
      "lb_categories": {
        "column": "created_by",
        "on_removed": "keep"
      },
      "lb_participants": {
        "column": "member_id",
        "on_removed": "keep"
      },
      "lb_ratings": {
        "column": "member_id",
        "on_removed": "delete"
      }
    },
  "resource_limits": {
    "max_db_bytes": 52428800
  },
  "row_policies": {
    "lb_categories": {
      "kind": "adult_writable"
    }
  },
  "icon": "🏆",
  "publishes": ["leaderboard.match_recorded"],
  "publish_acls": {
    "leaderboard.match_recorded": { "require_role": "member" }
  },
  "nav": { "label": "Leaderboard" },
  "glance": {
    "source": {
      "kind": "sql",
      "query": "SELECT c.name AS title, printf('%d W - %d L', r.wins, r.losses) AS subtitle, r.member_id AS member_id FROM app_leaderboard__lb_ratings r JOIN app_leaderboard__lb_categories c ON c.id = r.category_id WHERE r.games_played > 0 ORDER BY r.rating DESC LIMIT 3"
    },
    "display": { "template": "list", "title": "title", "subtitle": "subtitle", "owner": "member_id" }
  },
  "widget": { "label": "Rankings", "size": "medium" },
  "subscribes_to": ["game.completed"],
  "ai_access": {
    "allowed": true,
    "mode": "read",
    "db_exports": ["current_standings", "recent_matches"]
  }
}
